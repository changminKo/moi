{
  "name": "@skipjack/paper-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build && node --eval \"fs.rmSync('dist/db/migrations', { recursive: true, force: true }); fs.cpSync('src/db/migrations', 'dist/db/migrations', { recursive: true })\"",
    "test": "vitest run --passWithNoTests --dir src",
    "typecheck": "tsc --build --pretty false"
  },
  "dependencies": {
    "@fastify/cors": "11.3.0",
    "@fastify/helmet": "13.1.1",
    "@skipjack/trading-core": "workspace:*",
    "fastify": "5.12.1",
    "kysely": "0.29.5",
    "pg": "8.23.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "12.1.0",
    "@types/pg": "8.23.1",
    "vitest": "4.1.11"
  }
}
