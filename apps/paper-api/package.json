{
  "name": "@skipjack/paper-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --build --pretty false"
  }
}
